# eNest_sum

Experiment outputs for **NeST-VNN runs using the “sum” readout / aggregation variant** and related long-form result exports (`results/`, `long_results/`). Used for cross-method comparisons and supplementary analyses referenced from **`metrics_analysis/`**.

This folder may contain **only large result trees** on disk; training scripts may live in sibling model folders. See **`metrics_analysis/README.md`** for how pooled CSVs were produced.
